import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
  root: {
    marginTop: 20,
    marginBottom: 20,
  },
  filterGrid: {
    backgroundColor: '#131a21',
    padding: 15,
    borderRadius: 10,
  },
  contentGrid: {
    height: 500,
  },
}));
